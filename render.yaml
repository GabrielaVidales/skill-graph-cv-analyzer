services:
  - type: web
    name: cv-skill-graph
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: PORT
        value: 5000
    healthCheckPath: /
    autoDeploy: true
